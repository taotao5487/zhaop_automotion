# 可选的详情页提取规则。
# 不配置某个站点时，详情流水线会走通用提取：
# 1. 站点选择器（若存在）
# 2. readability-lxml 兜底
# 3. body 回退

sites:
  site_6_6:
    content_selectors:
      - "#vsb_content_500 .v_news_content"
      - "[id^='vsb_content_'] .v_news_content"
      - "#vsb_content_500"
    remove_selectors:
      - ".page-date"
      - ".bdsharebuttonbox"
      - ".fixed"
      - ".con-banner"
    attachment_selectors:
      - "#vsb_content_500 a[href]"
      - "[id^='vsb_content_'] a[href]"

  site_13_13:
    content_selectors:
      - ".newdet .newdet-det"
    remove_selectors:
      - ".in-wz"
      - ".newdet-bt p"
    attachment_selectors:
      - ".newdet .newdet-det a[href]"

  site_144_144:
    content_selectors:
      - ".art .content"
    remove_selectors:
      - ".fenxiang"
      - ".dqwz"
      - ".ejlm_m"
      - ".nybanner"
      - "script[src*='gethits.asp']"
    attachment_selectors:
      - ".art .content a[href]"

  site_72_72:
    content_selectors:
      - ".c13_r .nr1 .zw"
    remove_selectors:
      - ".bt h3"
    attachment_selectors:
      - ".c13_r .nr1 .zw a[href]"

  site_84_84:
    content_selectors:
      - ".work_main_left .work_del_content"
    remove_selectors:
      - ".work_del_date"
      - ".next_art"
      - ".art_btn"
    attachment_selectors:
      - ".work_main_left .work_del_content a[href]"

  site_118_118:
    content_selectors:
      - ".newsInfo .infoLeft .dis_content"
    remove_selectors:
      - ".dis_span"
      - ".ziyeNav"
    attachment_selectors:
      - ".newsInfo .infoLeft .dis_content a[href]"
